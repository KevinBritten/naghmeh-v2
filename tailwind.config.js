// tailwind.config.js

module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ["Montserrat", "sans-serif"], // Montserrat for sans-serif elements
        serif: ["Georgia", "serif"], // Georgia for serif elements (example)
      },
    },
  },
  plugins: [
    // Other plugins if needed
  ],
};
